const Engineer = require('../lib/Engineer');

describe("Engineer", ()=>{
    describe("manager details", ()=>{
        it("return a manager's name, id, and email", ()=>{
            expect()
        }
        )
    }) 
})